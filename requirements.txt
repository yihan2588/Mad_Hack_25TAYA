# Core numeric / MIDI
numpy>=1.24
pretty_midi>=0.2.10
mido>=1.3.2
unidecode>=1.3.8

# Audio I/O used in transcription step
librosa>=0.10.2
soundfile>=0.12.1
audioread>=3.0.1
soxr>=0.3.7

# Evaluation / utilities (from Colab)
mir_eval>=0.7

# Scientific stack MUSC pulls in at import/runtime
scipy>=1.9
pandas>=2.0
matplotlib>=3.7
tqdm>=4.66
einops>=0.7

# MUSC uses gdown to fetch weights
gdown>=4.7.1

# Torch audio stack (match HF base torch)
torch==2.8.0
torchaudio==2.8.0
torchcodec>=0.0.2